"use client";
import {AnimatePresence,motion} from "framer-motion";import{useEffect,useMemo,useState}from"react";import{runBattle}from"@/lib/scoring/battle-engine";import{collectDeviceProfile,type DeviceProfile}from"@/lib/device/device-profile";import{BATTLE_STAGES,ROUND_DURATION_MS}from"@/lib/battles/stages";import type{Phone}from"@/types/battle";
const labels={performance:"Performance",camera:"Camera",display:"Display",battery:"Battery",storage:"Storage",connectivity:"Connectivity"} as const;const icons={performance:"⚡",camera:"📸",display:"◉",battery:"⌁",storage:"▣",connectivity:"⌁"} as const;
type SpecStage=keyof typeof labels;
function isSpecStage(stage: string): stage is SpecStage{return stage in labels}
async function resolve(params:string){const r=await fetch(`/api/device-specs?${params}`,{cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error||"Device lookup failed");return d.device as Phone}
export default function BattleArena(){const[profile,setProfile]=useState<DeviceProfile|null>(null);const[me,setMe]=useState<Phone|null>(null);const[opponent,setOpponent]=useState<Phone|null>(null);const[query,setQuery]=useState("iPhone 17 Pro");const[busy,setBusy]=useState(true);const[error,setError]=useState("");const[playing,setPlaying]=useState(false);const[index,setIndex]=useState(-1);useEffect(()=>{collectDeviceProfile().then(async p=>{setProfile(p);if(p.model){try{setMe(await resolve(`model=${encodeURIComponent(p.model)}`))}catch(e){setError(e instanceof Error?e.message:"Unable to resolve your phone")}}else setError("Your browser did not expose a device model. Enter your phone model below.")}).finally(()=>setBusy(false))},[]);const result=useMemo(()=>me&&opponent?runBattle(me,opponent):null,[me,opponent]);useEffect(()=>{if(!playing||!result)return;const t=window.setTimeout(()=>setIndex(i=>i>=BATTLE_STAGES.length-1?0:i+1),ROUND_DURATION_MS);return()=>window.clearTimeout(t)},[index,playing,result]);const start=async()=>{setError("");setBusy(true);try{const p=opponent?.name===query?opponent:await resolve(`name=${encodeURIComponent(query)}`);setOpponent(p);if(!me)throw new Error("Your phone could not be resolved yet");setIndex(0);setPlaying(true)}catch(e){setError(e instanceof Error?e.message:"Unable to load opponent")}finally{setBusy(false)}};const stage=index<0?"idle":BATTLE_STAGES[index];const specStage=isSpecStage(stage)?stage:null;const round=specStage&&result?result.rounds.find(r=>r.spec===specStage):undefined;const title=stage==="battle-start"?"BATTLE START":stage==="final"?"FINAL VERDICT":stage==="idle"?"READY":specStage?labels[specStage]:"READY";const progress=index<0?0:index/(BATTLE_STAGES.length-1)*100;const meLabel=me?`${me.brand} ${me.name}`:profile?.model||"Your phone";return <main className="arena"><header><div className="brand">SPEC<span>BATTLE</span></div><div className="live"><i/>{busy?"SCANNING":"DEVICE READY"}</div></header><section className="combatants"><div className="combatant"><div className="phone-orb">A</div><div><b>{meLabel}</b><small>{profile?`${profile.hardware.logicalCores??"?"} CORES · ${profile.screen.width}×${profile.screen.height}`:"DETECTING DEVICE"}</small></div></div><div className="versus">VS</div><div className="combatant right"><div><b>{opponent?.brand?`${opponent.brand} ${opponent.name}`:"CHOOSE OPPONENT"}</b><small>{opponent?"VERIFIED DEVICE": "TYPE A PHONE MODEL"}</small></div><div className="phone-orb">B</div></div></section><section className="control"><input value={query} onChange={e=>{setQuery(e.target.value);setPlaying(false);setIndex(-1)}} aria-label="Opponent phone model" placeholder="e.g. Samsung Galaxy S25 Ultra"/><button onClick={start} disabled={busy||!query.trim()}>BATTLE</button>{error&&<p>{error}</p>}</section><section className="arena-stage"><AnimatePresence mode="wait">{!playing||!result?<motion.div className="battle-card start-card" key="ready" initial={{opacity:0,scale:.9}} animate={{opacity:1,scale:1}}><div className="cross">⚔</div><span className="round-label">{me?"DEVICE READY":"DEVICE SCAN"}</span><h1>{me?"READY TO BATTLE":"READING YOUR PHONE"}</h1><p>{me?"ENTER AN OPPONENT AND START THE FIGHT":"USING YOUR BROWSER'S EXPOSED DEVICE DATA"}</p></motion.div>:stage==="final"?<motion.div className="battle-card final-card" key="final" initial={{opacity:0,scale:.82}} animate={{opacity:1,scale:1}} exit={{opacity:0}}><div className="spec-icon">🏆</div><span className="round-label">FINAL VERDICT</span><h1>{result.winner==="draw"?"DRAW":`${result.winner==="left"?me!.name:opponent!.name} WINS`}</h1><div className="final-score"><strong>{result.leftTotal}</strong><span>—</span><strong>{result.rightTotal}</strong></div></motion.div>:stage==="battle-start"?<motion.div className="battle-card start-card" key="start" initial={{opacity:0,scale:.75}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:1.12}}><div className="cross">⚔</div><span className="round-label">READY</span><h1>BATTLE START</h1><p>{me!.name} VS {opponent!.name}</p></motion.div>:round&&specStage?<motion.div className="battle-card" key={specStage} initial={{opacity:0,x:260,rotate:4}} animate={{opacity:1,x:0,rotate:0}} exit={{opacity:0,x:-260,rotate:-4}} transition={{type:"spring",stiffness:180,damping:20}}><div className="spec-icon">{icons[specStage]}</div><span className="round-label">SPEC ROUND</span><h1>{title}</h1><div className="duel-score"><div className={round.winner==="left"?"champ":""}><strong>{round.leftScore}</strong><span>{me!.name}</span></div><em>VS</em><div className={round.winner==="right"?"champ":""}><strong>{round.rightScore}</strong><span>{opponent!.name}</span></div></div><div className="round-result">{round.winner==="draw"?"DRAW":`${round.winner==="left"?me!.name:opponent!.name} TAKES THE ROUND`}</div></motion.div>:null}</AnimatePresence></section><footer><span>ROUND {Math.max(0,Math.min(index,6))} / 6</span><div className="progress"><motion.div animate={{width:`${progress}%`}}/></div><span>{title.toUpperCase()}</span></footer></main>}
